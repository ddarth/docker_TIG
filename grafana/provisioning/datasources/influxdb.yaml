apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    uid: ce7aji2ql3shsd
    access: proxy
    url: http://influxdb:8086
    database: influx
    user: $INFLUXDB_ADMIN_USER
    editable: false
    jsonData:
      httpMode: POST
      version: "InfluxQL"
    secureJsonData:
      password: $INFLUXDB_ADMIN_PASSWORD
